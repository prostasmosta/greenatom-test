en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            name:
              blank: "can't be blank"
            email:
              blank: "can't be blank"
              taken: "has already been taken"
              invalid: "is invalid"
            passport_data:
              invalid_format: "has invalid format"
              invalid_number: "must contain a valid number"
              invalid_issued_by: "must contain a valid issued_by"
              invalid_issued_at: "must contain a valid issued_at"
  errors:
    messages:
      invalid_token: 'invalid token'